# MCP Toolkit Server Configuration
# Copy to .env and fill in values for production mode

# Server mode: mock (default), production, hybrid
MCP_MODE=mock

# OpenAI API key (required for production text analysis)
# OPENAI_API_KEY=sk-...

# Weather API key (required for production weather)
# WEATHER_API_KEY=...

# Server settings
MCP_SERVER_NAME=mcp-toolkit-server
MCP_SERVER_VERSION=0.1.0
MCP_MAX_CONCURRENT=5
MCP_TOOL_TIMEOUT_MS=30000
